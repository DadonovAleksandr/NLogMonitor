# nLogMonitor Environment Configuration
# Скопируйте этот файл в .env и настройте значения

# =============================================================================
# Application Mode
# =============================================================================

# Режим работы приложения: Web | Desktop
# Web: работа через браузер, загрузка файлов через upload (до 100MB)
# Desktop: нативное приложение Photino, прямой доступ к файловой системе
# По умолчанию: Web
APP_MODE=Web

# =============================================================================
# API Server
# =============================================================================

# URL и порт для API сервера
# Для production рекомендуется использовать reverse proxy (nginx)
# Для Desktop режима порт выбирается автоматически
ASPNETCORE_URLS=http://localhost:5000

# Окружение: Development | Production
# Development: включает Swagger UI, детальные ошибки
# Production: отключает Swagger, минимизирует информацию об ошибках
ASPNETCORE_ENVIRONMENT=Production

# =============================================================================
# Session Settings
# =============================================================================

# Fallback TTL для сессий в минутах
# Сессия удаляется после отключения последнего клиента + этот таймаут
# Используется как страховка при потере соединения (crash браузера, потеря сети)
SESSION_TTL_MINUTES=5

# =============================================================================
# File Settings
# =============================================================================

# Максимальный размер загружаемого файла в мегабайтах
# Рекомендуется не более 100MB для стабильной работы
MAX_FILE_SIZE_MB=100

# Директория для временных файлов (загруженные логи)
# Относительный путь от корня приложения или абсолютный путь
# Автоматически очищается при удалении сессии
TEMP_DIRECTORY=./temp

# =============================================================================
# Recent Logs Settings
# =============================================================================

# Максимальное количество записей в истории недавних файлов
RECENT_LOGS_MAX_ENTRIES=20

# Кастомный путь для хранения истории (опционально)
# По умолчанию:
# - Web режим: история хранится в памяти (не сохраняется между перезапусками)
# - Desktop режим: %APPDATA%/nLogMonitor/recent.json (Windows) или ~/.config/nLogMonitor/recent.json (Linux/macOS)
# Оставьте пустым для использования значений по умолчанию
RECENT_LOGS_STORAGE_PATH=

# =============================================================================
# CORS Settings
# =============================================================================

# Разрешенные origins для CORS (через запятую)
# Для development: http://localhost:5173 (Vite dev server)
# Для production: укажите домен вашего frontend
# Для Desktop режима: CORS не требуется (embedded ASP.NET Core)
CORS_ORIGINS=http://localhost:5173
