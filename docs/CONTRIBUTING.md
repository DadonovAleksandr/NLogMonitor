# 🤝 Contributing Guide

Спасибо за интерес к проекту NLogMonitor! Мы приветствуем любой вклад.

## 📋 Содержание

- [Кодекс поведения](#-кодекс-поведения)
- [Как внести вклад](#-как-внести-вклад)
- [Настройка окружения](#-настройка-окружения)
- [Процесс разработки](#-процесс-разработки)
- [Стиль кода](#-стиль-кода)
- [Коммиты](#-коммиты)
- [Pull Request](#-pull-request)

---

## 📜 Кодекс поведения

Участвуя в проекте, вы соглашаетесь соблюдать уважительное отношение ко всем участникам. Мы стремимся создать дружелюбную и инклюзивную среду.

---

## 💡 Как внести вклад

### Сообщить о баге

1. Проверьте [существующие issues](https://github.com/YOUR_USERNAME/NLogMonitor/issues)
2. Создайте новый issue с меткой `bug`
3. Опишите:
   - Ожидаемое поведение
   - Фактическое поведение
   - Шаги воспроизведения
   - Версия и окружение

### Предложить фичу

1. Создайте issue с меткой `enhancement`
2. Опишите функциональность и её пользу
3. Дождитесь обсуждения перед реализацией

### Улучшить документацию

- Исправления опечаток — создавайте PR напрямую
- Новые разделы — сначала создайте issue для обсуждения

### Написать код

1. Выберите issue с меткой `good first issue` или `help wanted`
2. Оставьте комментарий, что берёте задачу
3. Следуйте процессу разработки ниже

---

## 🛠️ Настройка окружения

### Требования

- .NET 10.0 SDK
- Node.js 20+
- Git
- VS Code / Rider (рекомендуется)

### Шаги

```bash
# 1. Fork репозитория на GitHub

# 2. Клонируйте свой fork
git clone https://github.com/YOUR_USERNAME/NLogMonitor.git
cd NLogMonitor

# 3. Добавьте upstream
git remote add upstream https://github.com/ORIGINAL_OWNER/NLogMonitor.git

# 4. Установите зависимости
dotnet restore
cd client && npm install

# 5. Запустите проект
dotnet run --project src/NLogMonitor.Api
# В другом терминале:
cd client && npm run dev
```

---

## 🔄 Процесс разработки

### 1. Синхронизация с upstream

```bash
git fetch upstream
git checkout master
git merge upstream/master
```

### 2. Создание ветки

```bash
# Формат: тип/краткое-описание
git checkout -b feature/dark-mode
git checkout -b fix/parser-multiline
git checkout -b docs/api-examples
```

### 3. Разработка

- Пишите код
- Добавляйте тесты
- Обновляйте документацию при необходимости

### 4. Проверка

```bash
# Backend
dotnet build
dotnet test

# Frontend
cd client
npm run lint
npm run type-check
npm run test
```

### 5. Коммит и Push

```bash
git add .
git commit -m "feat: add dark mode toggle"
git push origin feature/dark-mode
```

### 6. Создание Pull Request

---

## 🎨 Стиль кода

### C# (Backend)

- Используйте file-scoped namespaces
- Следуйте .NET naming conventions
- Запускайте `dotnet format` перед коммитом

```csharp
// ✅ Хорошо
namespace NLogMonitor.Application.Services;

public class LogService : ILogService
{
    private readonly ISessionStorage _storage;

    public LogService(ISessionStorage storage)
    {
        _storage = storage;
    }

    public async Task<PagedResult<LogEntry>> GetLogsAsync(
        Guid sessionId,
        LogFilterDto filter)
    {
        // ...
    }
}

// ❌ Плохо
namespace NLogMonitor.Application.Services
{
    public class logService
    {
        ISessionStorage storage;
        // ...
    }
}
```

### TypeScript/Vue (Frontend)

- Используйте Composition API с `<script setup>`
- Типизируйте все props и emits
- Запускайте `npm run lint:fix` перед коммитом

```vue
<!-- ✅ Хорошо -->
<script setup lang="ts">
import type { LogEntry } from '@/types';

interface Props {
  logs: LogEntry[];
  isLoading: boolean;
}

const props = defineProps<Props>();

const emit = defineEmits<{
  pageChange: [page: number];
}>();
</script>

<!-- ❌ Плохо -->
<script setup>
const props = defineProps(['logs', 'isLoading']);
</script>
```

---

## 📝 Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<тип>(<область>): <описание>

[тело]

[футер]
```

### Типы

| Тип | Описание |
|-----|----------|
| `feat` | Новая функциональность |
| `fix` | Исправление бага |
| `docs` | Изменения документации |
| `style` | Форматирование (без изменения логики) |
| `refactor` | Рефакторинг кода |
| `test` | Добавление/изменение тестов |
| `chore` | Обслуживание (deps, configs) |

### Примеры

```bash
# Новая фича
git commit -m "feat(parser): add support for Serilog format"

# Исправление
git commit -m "fix(api): handle empty file upload"

# Документация
git commit -m "docs(readme): add Docker instructions"

# С телом и ссылкой на issue
git commit -m "fix(ui): prevent table flicker on filter change

Добавлено debounce для фильтров, что предотвращает
множественные запросы при быстром изменении фильтров.

Closes #42"
```

---

## 🔀 Pull Request

### Чеклист перед созданием PR

- [ ] Код соответствует стилю проекта
- [ ] Все тесты проходят
- [ ] Добавлены тесты для новой функциональности
- [ ] Обновлена документация (если нужно)
- [ ] Коммиты следуют Conventional Commits
- [ ] Ветка основана на актуальном `master`

### Шаблон PR

```markdown
## Описание

Краткое описание изменений.

## Тип изменения

- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation

## Как тестировать

1. Шаг 1
2. Шаг 2
3. Ожидаемый результат

## Скриншоты (если применимо)

## Связанные issues

Closes #123
```

### После создания PR

1. Дождитесь автоматических проверок (CI)
2. Ответьте на комментарии ревьюеров
3. Внесите запрошенные изменения
4. После аппрува — PR будет смержен

---

## ❓ Вопросы?

- Создайте [Discussion](https://github.com/YOUR_USERNAME/NLogMonitor/discussions)
- Или задайте вопрос в issue с меткой `question`

---

## 🙏 Благодарности

Спасибо всем контрибьюторам!

<!--
Список контрибьюторов будет автоматически обновляться:
https://github.com/YOUR_USERNAME/NLogMonitor/graphs/contributors
-->
